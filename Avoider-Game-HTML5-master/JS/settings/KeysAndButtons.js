// =======================================================
// GameKeysAndButtons.js
// -------------------------------------------------------
// Purpose:
// Immutable keyboard keys and gamepad buttons settings
// =======================================================

const keyTypes = 
{
    PLAY_KEY:    "Space",
    RESET_KEY:   "Space",
}

Object.freeze(keyTypes);